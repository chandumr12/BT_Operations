rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Batch documents: public read (leads can download), auth write (admin uploads)
    match /batch-documents/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
