# ── Dependencies (regenerated by npm install / pip install) ──
node_modules/
frontend/node_modules/

# ── Python virtual environment ──
venv/
.venv/
backend/venv/
backend/.venv/
__pycache__/
*.pyc
*.pyo

# ── Build outputs ──
/build
frontend/build/
dist/
.next/
/out/
*.tsbuildinfo

# ── Firebase / Google credentials (NEVER commit these) ──
firebase-key.json
firebase-admin.json
*-key.json
*credentials*.json
*serviceAccount*.json
*.pem

# ── Environment variables ──
.env
.env.local
.env.development
.env.staging
.env.production
.env.development.local
.env.test.local
.env.production.local
*.env

# ── Stray files ──
,

# ── Misplaced folders ──
okay/

# ── Logs ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── OS files ──
.DS_Store
Thumbs.db

# ── Editor ──
.vscode/
.idea/

# ── Caches ──
.cache/
.yarn/install-state.gz
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
.pnp
.pnp.js

# ── Coverage ──
/coverage

# ── Archives ──
*.zip
*.tar.gz
*.tar
*.tgz
*.pack
*.deb
*.dylib
